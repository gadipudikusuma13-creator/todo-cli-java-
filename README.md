# CLI Task Manager - Java

A command-line to-do app in Java with full CRUD operations.

## Setup
javac TodoApp.java -d out

## Run
java -cp out TodoApp add "Buy groceries"
java -cp out TodoApp list
java -cp out TodoApp complete 1
java -cp out TodoApp delete 1
